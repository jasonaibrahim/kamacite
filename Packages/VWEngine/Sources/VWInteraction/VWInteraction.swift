// VWInteraction — hit testing, selection, copy, links (lands in P6).
//
// Point → block → CTLineGetStringIndexForPosition; anchor+focus selection over
// TextPosition order; ⌘C plain text and ⌘⇧C byte-exact markdown source (nearly free
// because SourceSpans thread through the pipeline); link regions + hover cursor.
