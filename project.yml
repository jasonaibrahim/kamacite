name: kamacite
options:
  bundleIdPrefix: com.xylophonexyz
  deploymentTarget:
    macOS: "15.0"
  createIntermediateGroups: true

packages:
  VWEngine:
    path: Packages/VWEngine

settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "15.0"
    DEAD_CODE_STRIPPING: YES
    CODE_SIGN_STYLE: Manual
    CODE_SIGN_IDENTITY: "-"
    ENABLE_HARDENED_RUNTIME: NO
    ENABLE_USER_SCRIPT_SANDBOXING: NO
  configs:
    Release:
      SWIFT_OPTIMIZATION_LEVEL: "-O"
      SWIFT_COMPILATION_MODE: wholemodule

targets:
  kamacite:
    type: application
    platform: macOS
    sources:
      - App/Sources
      - path: App/Resources/Kamacite.icns
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.xylophonexyz.kamacite
        PRODUCT_NAME: Kamacite
        SWIFT_DEFAULT_ACTOR_ISOLATION: MainActor
    dependencies:
      - package: VWEngine
        product: VWViewer
      - target: kama-cli
        embed: false
    info:
      path: App/Info.plist
      properties:
        CFBundleName: Kamacite
        CFBundleDisplayName: Kamacite
        CFBundleIconFile: Kamacite
        CFBundleShortVersionString: "1.0.0-beta.1"
        CFBundleVersion: "1"
        LSMinimumSystemVersion: "15.0"
        LSApplicationCategoryType: public.app-category.productivity
        NSPrincipalClass: NSApplication
        NSHighResolutionCapable: true
        NSSupportsAutomaticGraphicsSwitching: true
        NSHumanReadableCopyright: "© 2026 xylophonexyz"
        CFBundleDocumentTypes:
          # No system markdown UTI exists (verified against this machine's LS database).
          # Import the canonical community UTI and claim the variants other apps declare,
          # so Finder double-click and "Open With" work regardless of which app won the
          # extension binding. Referencing a UTI another app declares is legal and inert
          # if undeclared.
          - CFBundleTypeName: Markdown Document
            CFBundleTypeRole: Viewer
            LSHandlerRank: Default
            LSItemContentTypes:
              - net.daringfireball.markdown
              - public.markdown
              - net.ia.markdown
              - com.unknown.md
          - CFBundleTypeName: Plain Text
            CFBundleTypeRole: Viewer
            LSHandlerRank: Alternate
            LSItemContentTypes:
              - public.plain-text
        UTImportedTypeDeclarations:
          - UTTypeIdentifier: net.daringfireball.markdown
            UTTypeDescription: Markdown Document
            UTTypeConformsTo:
              - public.plain-text
            UTTypeTagSpecification:
              public.filename-extension:
                - md
                - markdown
                - mdown
                - mkdn
                - mkd
                - mdwn
                - markdn
              public.mime-type:
                - text/markdown
                - text/x-markdown
    postBuildScripts:
      - name: Embed kama CLI helper
        script: |
          set -e
          HELPERS="${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/Helpers"
          mkdir -p "$HELPERS"
          cp -f "${TARGET_BUILD_DIR}/kama-cli" "$HELPERS/kama"
          # The helper must carry its own signature before the outer bundle is sealed.
          codesign --force --sign - "$HELPERS/kama"
        basedOnDependencyAnalysis: false

  kama-cli:
    type: tool
    platform: macOS
    sources:
      - CLI/Sources
    settings:
      base:
        PRODUCT_NAME: kama-cli
        SWIFT_DEFAULT_ACTOR_ISOLATION: nonisolated

schemes:
  kamacite:
    build:
      targets:
        kamacite: all
    run:
      config: Release
